version: '3.8'

services:
  douyin-spark:
    build: .
    container_name: douyin-spark-flow
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./.env:/app/.env
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
